import { AddLocationPipe } from './add-location.pipe';

describe('AddLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new AddLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
